#include <stdio.h>
#include <stdlib.h>
#include <string.h>


int main(int argc, char** argv)
{
   int testCases;
   scanf("%i", &testCases);
   for(int i = 0; i < t; i++) {
	   int hotValues;
	   scanf("%i",&hotValues);
	   int woman[hotValues];
	   int man[hotValues];
	   char manValues[255];
	   scanf("%s", manValues);
	   char womanValues[255];
	   scanf("%s", womanValues);
	   for(int j = 0; j < len(manValues); j++) {
		   
	   }
   }
   return 0;
}
